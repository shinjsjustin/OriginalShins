// Loaded automatically by react-scripts before every test file.
// Adds the jest-dom matchers (toBeInTheDocument, toHaveTextContent, ...) that
// the component tests assert with.
import '@testing-library/jest-dom';
