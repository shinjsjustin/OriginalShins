Initialize this freshly-unzipped template as a new project: read INIT.md at the repository root and follow its steps exactly (ask for project name, database name, and port; run ./init.sh; verify; report; offer cleanup).
